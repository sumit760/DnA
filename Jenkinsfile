pipeline {
    agent any

    stages {
        /*stage('Git Push') {
            steps {
                echo 'git merge SANDBOX to Dev..'
            }
        }*/
        stage('Pull to IICS org') {
            steps {
                echo 'pullByCommitHash..'
            }
        }
    }
}
